'use strict';

/**
 * resturant-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::resturant-category.resturant-category');
